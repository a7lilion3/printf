#ifndef HEADER
#define HEADER

#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>

int _putchar(int c);
int _puts(char *s);

/* _printf prototype */
int _printf(const char *format, ...);

#endif
